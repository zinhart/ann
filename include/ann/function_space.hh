#ifndef FUNCTION_SPACE_HH
#define FUNCTION_SPACE_HH
#include <cstdint>
namespace zinhart
{
  namespace function_space
  {
	enum objective : std::uint32_t;
	enum derivative : std::uint32_t;
  }
}
#endif
